@model WebApplicationlaptop.Models.BrandModel

@{
    ViewData["Title"] = "Thêm thương hiệu";
}

<h2>Thêm Thương hiệu</h2>

<div class="col-md-7">
    <form asp-action="AddCategories" enctype="multipart/form-data">
        <div asp-validation-summary="ModelOnly" class="text-danger"></div>

        <div class="form-group">
            <label asp-for="Name">Tên Thương hiệu:</label>
            <input asp-for="Name" class="form-control" />
            <span asp-validation-for="Name" class="text-danger"></span>
        </div>

        <div class="form-group">
            <label asp-for="Description">Mô Tả:</label>
            <textarea asp-for="Description" class="form-control" rows="5"></textarea>
            <span asp-validation-for="Description" class="text-danger"></span>
        </div>

        <div class="form-group">
            <label asp-for="status">Status thương hiệu :</label>
            <select asp-for="status" class="form-control">
                <option value="1">-- :Hiển thị--</option>
                <option value="0">--Ẩn --</option>
            </select>
            <span asp-validation-for="status" class="text-danger"></span>
        </div>

        <button type="submit" class="btn btn-primary">Thêm Thương hiệu</button>
    </form>
    <a asp-action="index">Quay về  thêm thương hiệu</a>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
    <script src="https://cdn.ckeditor.com/ckeditor5/41.4.2/classic/ckeditor.js"></script>
    <script>
        ClassicEditor
            .create(document.querySelector('#Description'))
            .catch(error => {
                console.error(error);
            });
    </script>
}
